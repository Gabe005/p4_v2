http {
    upstream frontend {
        server localhost:3000;
        server localhost:3010;  # Second frontend instance
    }

    server {
        listen 80;
        
        location / {
            proxy_pass http://frontend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}